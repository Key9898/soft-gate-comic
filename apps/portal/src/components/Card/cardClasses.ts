/**
 * Class-string form of `Card`, for the places that style an existing element rather
 * than render a wrapper (section shells on the info pages, list rows, `<Link>` tiles).
 * Kept beside `Card` so the two cannot drift.
 */
export const CARD_CLASS = 'rounded-2xl border border-gray-200 bg-white shadow-sm'

export const PANEL_CLASS = 'rounded-3xl border border-gray-200/60 bg-white shadow-sm'
